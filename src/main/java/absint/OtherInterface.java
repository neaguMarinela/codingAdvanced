package absint;

public abstract interface OtherInterface {
    //private void method1(); //An interface could not have private method signature declared in it. It could have default, public, abstract, static. It won't compile.
}
